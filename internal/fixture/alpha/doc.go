// Package alpha defines an imaginary switch CLI fixture for blocks, toggles,
// physical-port resets, feature gates, list values, and dual-form membership.
// Its grammar derives from a real vendor grammar. See docs/fixtures.md for its
// origin and unverified behavior.
package alpha
